<%= form_with(model: @post, local: true) do |form| %>
 <div class="container">

      <div class="form-group">
      <%= form.label :invoice %>
      <%= form.file_field :header_image %>
      </div>

      <%= form.submit %>

      </div>
  <% end %>